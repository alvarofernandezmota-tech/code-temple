# Madre (servidor)

> Última verificación: [PENDIENTE — pegar aquí fecha + salida de comandos]

## Comandos de auditoría

```bash
ssh varopc@madre "hostname && uname -a"
ssh varopc@madre "docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'"
ssh varopc@madre "df -h && free -h"
```

## Estado real

(rellenar tras ejecutar los comandos de arriba)
